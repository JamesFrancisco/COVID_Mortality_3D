---
title: "COVID-19 U.S. morbidity"
author: "James R. Francisco"
date: "6/26/2020"
output:
  html_document: default
  word_document: default
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(pastecs)
library(gclus)
library(plotly)
library(dplyr)
library(jsonlite)

df <- read.csv("https://raw.githubusercontent.com/coviddata/coviddata/master/data/sources/ny_times/us-states.csv")
df2 <- read.csv("https://raw.githubusercontent.com/coviddata/coviddata/master/data/sources/ny_times/us-counties.csv")
df3 <- fromJSON("https://covidtracking.com/api/v1/states/daily.json", flatten = TRUE)
df3$date <- as.Date(as.character(df3$date), format("%Y%m%d"))
df <- df %>%
    group_by(state) %>%
    arrange(date) %>%
    mutate(deaths.day = deaths -lag(deaths, default = first(deaths))) %>%
    mutate(cases.day = cases -lag(cases, default = first(cases)))
df2 <- df2 %>%
    group_by(state, county) %>%
    arrange(date) %>%
    mutate(deaths.day = deaths -lag(deaths, default = first(deaths)) )%>%
    mutate(cases.day = cases -lag(cases, default = first(cases)))
df3 <- df3 %>%
    group_by(state) %>%
    arrange(date) %>%
    mutate(hospitalized.day = hospitalizedCumulative -lag(hospitalizedCumulative,        default = first(hospitalizedCumulative)))


```

## Ladies and Gentlemen, we've been had 

https://medium.com/analyticaper/covid-19-what-the-data-tells-us-3a08e42ee36f

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r, echo=FALSE}
plot_ly(df, x=~df$date, y=~df$state, z=~df$deaths.day, type="scatter3d", mode="markers", size=1 , name = "Test Data (Actual)", height = 750, width = 750)
```



```{r, echo=FALSE}

plot_ly(df3, x=~df3$date, y=~df3$state, z=~df3$hospitalized.day , type="scatter3d", mode="markers", size=1 , name = "Test Data (Actual)", height = 750, width = 750)

```


```{r, echo=FALSE}

plot_ly(df, x=~df$date, y=~df$state, z=~df$cases.day, type="scatter3d", mode="markers", size=1 , name = "Test Data (Actual)", height = 750, width = 750)

```


Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
